import { TimelineElementDirective } from './timeline-element.directive';

describe('TimelineElementDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineElementDirective();
    expect(directive).toBeTruthy();
  });
});
